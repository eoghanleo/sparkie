# Database Design (Domain)

Primary artifacts:
- `db-spec` — schema and data contract definitions that are safe to implement.

Canonical locations:
- Working artifacts: `phosphene/domains/database-design/output/`

Handoff (default):
- **From Product Architecture / Product Management** with domain model and constraints.
- **To Code Development** with a migration plan + query/index expectations.

