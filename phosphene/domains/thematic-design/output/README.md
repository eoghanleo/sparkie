# Thematic Design (Domain)

Primary artifacts:
- `theme-spec` — design tokens and theming constraints that can be implemented.

Canonical locations:
- Working artifacts: `phosphene/domains/thematic-design/output/`

Handoff (default):
- **From User Interface** with screens/flows that need a coherent visual system.
- **To Code Development** with implementable theming/tokens and accessibility constraints.

