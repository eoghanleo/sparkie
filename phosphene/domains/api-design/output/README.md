# API Design (Domain)

Primary artifacts:
- `api-spec` — contracts for APIs/events that can be implemented and tested.

Canonical locations:
- Working artifacts: `phosphene/domains/api-design/output/`

Handoff (default):
- **From Product Architecture / Product Management** with product requirements and domain model.
- **To Backend Development / Code Development** with implementable contracts and edge cases.

