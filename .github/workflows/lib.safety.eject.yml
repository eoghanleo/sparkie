name: "lib.safety.eject"

on:
  workflow_call: {}

permissions:
  pull-requests: write
  issues: write
  contents: read

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "TODO: reusable eject building block (PR close + branch delete + audit comment)."

